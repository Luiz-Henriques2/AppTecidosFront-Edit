export interface FornecedorInterface {
    id?: number,
    nome: string,
    email?: string,
    telefone?: string,
    whatsapp?: string,
    endereco?: string,
    site?: string,

    image?: string,
    
    created_at?: string,
    updated_at?: string,
}